spring:
  application:
    name: sf-usermanagement

  datasource:
    url: jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
    username: postgres.mbhhzfvfwkqwvznumjmm
    password: KZWf9xhXTUr80fxv
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        hbm2ddl:
          auto: none
        dialect: org.hibernate.dialect.PostgreSQLDialect
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8081

jwt:
  keystore:
    location: classpath:jwt-keystore.jks
    password: password
    alias: jwt-key
