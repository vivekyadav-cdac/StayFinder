spring:
  application:
    name: sf-property-service

  datasource:
    url: jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:5432/postgres
    username: postgres.qwxlricixjwljrdmvecy
    password: VivekCdac@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

gateway:
  base-url: http://apigateway:8000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

server:
  port: 8085

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 15MB